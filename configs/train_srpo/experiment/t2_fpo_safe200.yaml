# @package _global_

metadata:
  label: t2-fpo-safe200
  source_doc: docs/fpo_hyperparameter_experiments.md
  notes: "Task-2 stable reference from the FPO hyperparameter study: lr=3e-6, conservative clip, ppo_epochs=1, long run."

task_ids: "2"
mode: sparse_rl
update_method: fpo
lr: 3e-6
iterations: 200
eval_every: 10
eval_episodes: 50
wandb_name: t2-fpo-safe200-lr3e6-clip005

kl:
  coeff: 0.01
  sft_coeff: 0.0

ppo:
  epochs: 1
  clip_epsilon: 0.05
  clip_epsilon_high: 0.08

fpo:
  negative_adv_scale: 1.0
