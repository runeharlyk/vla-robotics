# @package _global_

metadata:
  label: spatial-sft-n50-seeded
  notes: "Seeded LIBERO spatial SFT eval executing the full 50-action chunk before replanning."

wandb_name: eval_sft_spatial_current_seed42_n50
n_action_steps: 50
