# @package _global_

metadata:
  label: spatial-sft-n10-seeded
  notes: "Seeded LIBERO spatial SFT eval executing 10 actions per sampled chunk."

wandb_name: eval_sft_spatial_current_seed42_n10
n_action_steps: 10
